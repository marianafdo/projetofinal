/* if(!pergunta) {
        mensagemDeErro;
    } else if (pergunta === 1) {
        let novoNome = prompt('Digite o novo nome do solicitante/responsável pela reserva da sala.'); // NOME
            if (!novoNome){
            mensagemDeErro;
            } else {
            console.log('Nome alterado com sucesso!');
    }}
        else if (pergunta === 2){ 
        let novaData = prompt('Digite a nova data da reserva da sala (DD/MM/AAAA)'); // DATA
            if (!novaData){
                mensagemDeErro;
            } else {
                console.log('Data alterada com sucesso!');
            }
        }
        else if (pergunta === 3){
        let novoHorario = prompt('Digite o novo horário da reserva da sala (HH:MM)'); // HORARIO
            if (!novoHorario) {
                mensagemDeErro;
            } else {
                console.log('Horário alterado com sucesso!');
            }
        }
        else if (pergunta === 4){
        let novaSala = prompt('Digite o número da nova sala de reuniões'); // SALA
            if (!novaSala) {
                mensagemDeErro;
            } else {
                console.log('Sala alterada com sucesso!');
            }
        }
        else if (pergunta === 5){
        let novaFinalidade = prompt('Digite a nova finalidade da reunião'); // FINALIDADE
            if (!novaFinalidade){
                mensagemDeErro;
            } else {
                console.log('Finalidade da reunião alterada com sucesso!');
            }
    }
    else {
        console.log('Erro, refaça a operação!');
    } */


    /* const outraReserva = prompt('Deseja fazer outra reserva? Digite S para SIM ou N para não').toUpperCase();
        if (outraReserva === 'S') {
            criarReserva();
        } else if (outraReserva === 'N') {
            console.log('Ok. Voltando ao menu inicial.');
            exibirMenu();
        } else {
            console.log('Opção inválida. Voltando ao menu incial.');
            exibirMenu();
        } */


          /*   let confirmacao = Number(prompt('Você já visualizou as reservas?\n1. Para SIM\n2. Para NÃO'))
    if(confirmacao === 2){
        visualizarReservas();
    } else if (confirmacao === 1) {
        } */
